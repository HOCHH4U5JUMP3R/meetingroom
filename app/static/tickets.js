const q=document.querySelector('#q'),list=document.querySelector('#list'),filters=document.querySelector('#filters');let tickets=[];let selected='';const esc=v=>String(v??'–').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const n=v=>String(v||'').toLowerCase();function badge(v){const s=n(v),c=s.includes('krit')||s.includes('störung')?'badge-danger':s.includes('offen')||s.includes('bearbeitung')?'badge-warning':'badge-success';return `<span class="badge ${c}">${esc(v)}</span>`}async function load(){const rooms=await fetch('/api/rooms').then(r=>r.json());const d=await Promise.all(rooms.map(r=>fetch(`/api/rooms/${r.id}`).then(x=>x.json()).catch(()=>null)));tickets=d.filter(Boolean).flatMap(r=>(r.tickets||[]).map(t=>({...t,room:r})));stats();renderFilters();render()}function stats(){const open=tickets.filter(t=>['offen','in bearbeitung'].includes(n(t.status)));document.querySelector('#open').textContent=open.length;document.querySelector('#critical').textContent=tickets.filter(t=>n(t.priority).includes('krit')||n(t.priority).includes('hoch')).length;document.querySelector('#closed').textContent=tickets.filter(t=>n(t.status).includes('erledigt')||n(t.status).includes('geschlossen')||n(t.status).includes('abgeschlossen')).length;document.querySelector('#rooms').textContent=new Set(tickets.map(t=>t.room.id)).size;const counts=(field)=>Object.entries(tickets.reduce((a,t)=>(a[t[field]||'Sonstiges']=(a[t[field]||'Sonstiges']||0)+1,a),{})).sort((a,b)=>b[1]-a[1]);const max=Math.max(1,...counts('category').map(x=>x[1]));document.querySelector('#cats').innerHTML=counts('category').slice(0,7).map(([k,v])=>`<div class="metric-line"><div><strong>${esc(k)}</strong><span>${v}</span></div><i><b style="width:${v/max*100}%"></b></i></div>`).join('');const ps=counts('priority');const pm=Math.max(1,...ps.map(x=>x[1]));document.querySelector('#priority').innerHTML=ps.map(([k,v])=>`<div class="metric-line"><div><strong>${esc(k)}</strong><span>${v}</span></div><i><b style="width:${v/pm*100}%"></b></i></div>`).join('')}function renderFilters(){filters.innerHTML=['','Offen','In Bearbeitung','Erledigt'].map(v=>`<button class="site-filter ${v===selected?'active':''}" data-v="${v}">${v||'Alle'}</button>`).join('');filters.querySelectorAll('button').forEach(b=>b.onclick=()=>{selected=b.dataset.v;renderFilters();render()})}function render(){const term=n(q.value),rows=tickets.filter(t=>(!selected||n(t.status)===n(selected))&&[t.ticket_number,t.subject,t.category,t.priority,t.responsible,t.description,t.room?.name,t.room?.site].some(v=>n(v).includes(term))).sort((a,b)=>b.id-a.id);if(!rows.length){list.innerHTML='<div class="v2-loading">Keine Tickets gefunden.</div>';return}list.innerHTML=`<div class="asset-row asset-head"><span>Ticket</span><span>Raum</span><span>Kategorie</span><span>Status</span><span>Priorität</span></div>`+rows.map(t=>`<a class="asset-row" href="/static/room-detail.html?id=${t.room.id}"><div><strong>#${esc(t.ticket_number)}</strong><small>${esc(t.subject)}</small></div><div><strong>${esc(t.room.name)}</strong><small>${esc([t.room.site,t.room.building,t.room.floor].filter(Boolean).join(' · '))}</small></div><div>${esc(t.category)}</div><div>${badge(t.status)}</div><div>${badge(t.priority)}</div></a>`).join('')}q.addEventListener('input',render);load().catch(e=>list.innerHTML=`<div class="v2-loading">Fehler: ${esc(e.message)}</div>`);