const list=document.querySelector('#list'),q=document.querySelector('#q');let models=[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));async function load(){const [modelRes,statsRes]=await Promise.all([fetch('/api/equipment-models'),fetch('/api/equipment-models/stats')]);if(!modelRes.ok||!statsRes.ok)throw new Error('Modelldaten konnten nicht geladen werden.');models=await modelRes.json();const stats=await statsRes.json();document.querySelector('#count').textContent=stats.models;document.querySelector('#manufacturers').textContent=stats.manufacturers;document.querySelector('#assigned').textContent=stats.assigned;document.querySelector('#coverage').textContent=`${stats.coverage.toLocaleString('de-DE',{maximumFractionDigits:1})} %`;render()}function render(){const term=(q.value||'').toLowerCase().trim(),rows=models.filter(m=>[m.name,m.manufacturer,m.model_number,m.category].some(v=>String(v||'').toLowerCase().includes(term)));list.innerHTML=rows.length?rows.map(m=>`<article class="model-card"><div class="model-image">${m.image_url?`<img src="${esc(m.image_url)}" alt="">`:'<span>NO IMAGE</span>'}</div><div class="model-body"><div class="eyebrow">${esc(m.category||'MODELL')}</div><h3>${esc(m.name)}</h3><p>${esc([m.manufacturer,m.model_number].filter(Boolean).join(' · ')||'Keine Herstellerangabe')}</p><small>${esc([m.size_inches?m.size_inches+' Zoll':'',m.mounting].filter(Boolean).join(' · '))}</small><div class="model-foot"><span>${m.equipment_count||0} Geräte</span><a class="btn" href="/static/model-form.html?id=${m.id}">Bearbeiten</a></div></div></article>`).join(''):'<div class="v2-loading">Keine Modelle gefunden.</div>'}q.addEventListener('input',render);load().catch(e=>list.innerHTML=`<div class="v2-loading">Fehler: ${esc(e.message)}</div>`);