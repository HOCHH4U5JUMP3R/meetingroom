const q=document.querySelector('#q'),list=document.querySelector('#list'),filters=document.querySelector('#filters');let assets=[];let selected='';
const esc=v=>String(v??'–').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const euro=v=>Number(v||0).toLocaleString('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0});
const norm=v=>String(v||'').toLowerCase();
async function load(){const rooms=await fetch('/api/rooms').then(r=>r.json());const details=await Promise.all(rooms.map(r=>fetch(`/api/rooms/${r.id}`).then(x=>x.json()).catch(()=>null)));assets=details.filter(Boolean).flatMap(r=>(r.equipment||[]).map(e=>({...e,room:r})));document.querySelector('#count').textContent=assets.length;document.querySelector('#active').textContent=assets.filter(e=>!['außer betrieb','defekt','ausgemustert'].some(x=>norm(e.status).includes(x))).length;document.querySelector('#display').textContent=assets.filter(e=>norm(e.category).includes('display')||norm(e.category).includes('monitor')||norm(e.category).includes('bildschirm')).length;document.querySelector('#cost').textContent=euro(assets.reduce((n,e)=>n+Number(e.purchase_price||0),0));renderFilters();render()}
function renderFilters(){const cats=[...new Set(assets.map(e=>e.category).filter(Boolean))].sort();filters.innerHTML='<button class="site-filter active" data-v="">Alle</button>'+cats.map(c=>`<button class="site-filter" data-v="${esc(c)}">${esc(c)}</button>`).join('');filters.querySelectorAll('button').forEach(b=>b.onclick=()=>{selected=b.dataset.v;filters.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));render()})}
function render(){const term=norm(q.value);const rows=assets.filter(e=>(!selected||e.category===selected)&&[e.name,e.category,e.manufacturer,e.model,e.serial,e.inventory_number,e.host_name,e.room?.name,e.room?.site].some(v=>norm(v).includes(term)));if(!rows.length){list.innerHTML='<div class="v2-loading">Keine Geräte gefunden.</div>';return}list.innerHTML=`<div class="asset-row asset-head"><span>Gerät</span><span>Raum</span><span>Inventar / Host</span><span>Status</span><span>Wert</span></div>`+rows.map(e=>`<a class="asset-row" href="/static/room-detail.html?id=${e.room.id}"><div><strong>${esc(e.name)}</strong><small>${esc([e.manufacturer,e.model].filter(Boolean).join(' · ')||e.category)}</small></div><div><strong>${esc(e.room.name)}</strong><small>${esc([e.room.site,e.room.building,e.room.floor].filter(Boolean).join(' · '))}</small></div><div><strong>${esc(e.inventory_number||'–')}</strong><small>${esc(e.host_name||e.serial||'Keine Kennung')}</small></div><div>${badge(e.status||'Aktiv')}</div><strong>${euro(e.purchase_price)}</strong></a>`).join('')}
function badge(v){const s=norm(v),c=s.includes('defekt')||s.includes('störung')?'badge-danger':s.includes('offen')||s.includes('wart')?'badge-warning':'badge-success';return `<span class="badge ${c}">${esc(v)}</span>`}q.addEventListener('input',render);load().catch(e=>{list.innerHTML=`<div class="v2-loading">Fehler: ${esc(e.message)}</div>`});