const form=document.querySelector('#entityForm'),config=JSON.parse(document.querySelector('#formConfig').textContent),params=new URLSearchParams(location.search);let roomId=Number(params.get('room_id'))||0,entryId=Number(params.get('id'))||0,selectedModelId=null,models=[];
const defs={serial:['Seriennummer','text'],inventory_number:['Inventarnummer','text'],host_name:['Host-Name','text'],mac_address:['MAC-Adresse','text'],ip_address:['IP-Adresse','text'],firmware:['Firmware','text'],size_inches:['Größe (Zoll)','number'],mounting:['Montage','select'],purchase_date:['Kaufdatum','date'],purchase_price:['Kaufpreis (€)','number'],responsible:['Verantwortlicher','text']};
const defaults={'monitor':['serial','inventory_number','size_inches','mounting','purchase_date','purchase_price'],'tv / display':['serial','inventory_number','size_inches','mounting','purchase_date','purchase_price'],'vc-system':['serial','inventory_number','host_name','mac_address','ip_address','firmware','mounting','purchase_date','purchase_price'],'kamera':['serial','inventory_number','host_name','mac_address','ip_address','firmware','mounting','purchase_date','purchase_price'],'mikrofon':['serial','inventory_number','mounting','purchase_date','purchase_price'],'lautsprecher':['serial','inventory_number','mounting','purchase_date','purchase_price'],'pc':['serial','inventory_number','host_name','mac_address','ip_address','firmware','purchase_date','purchase_price'],'netzwerk':['serial','inventory_number','host_name','mac_address','ip_address','firmware','purchase_date','purchase_price'],'steuerung':['serial','inventory_number','host_name','mac_address','ip_address','firmware','mounting','purchase_date','purchase_price']};
const message=(text,type='error')=>{const e=document.querySelector('#formMessage');e.textContent=text;e.className=`form-message ${type}`};const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function renderDynamic(keys=[],values={}){const box=document.querySelector('#dynamicFields');if(!box)return;box.innerHTML=keys.map(key=>{const [label,type]=defs[key]||[key,'text'];let input=type==='select'?`<select data-dynamic="${key}"><option value="">Bitte auswählen</option><option>Wand</option><option>Decke</option><option>Tisch</option><option>Ständer</option><option>Rack</option><option>Einbau</option><option>Mobil</option></select>`:`<input data-dynamic="${key}" type="${type}" ${key==='purchase_price'?'min="0" step="0.01"':''}>`;return `<div class="field"><label>${label}</label>${input}</div>`}).join('');box.querySelectorAll('[data-dynamic]').forEach(i=>i.value=values[i.dataset.dynamic]??'')}
function modelKeys(m){if(m?.field_config?.length)return m.field_config;const c=String(m?.category||form.elements.category.value||'').toLowerCase();return defaults[c]||[]}
function showModel(m){const p=document.querySelector('#modelPreview');if(!p)return;p.innerHTML=m?`${m.image_url?`<img src="${esc(m.image_url)}" alt="">`:''}<div><strong>${esc([m.manufacturer,m.model_number].filter(Boolean).join(' · ')||m.name)}</strong><small>${esc([m.category,m.size_inches?m.size_inches+' Zoll':'',m.mounting].filter(Boolean).join(' · '))}${m.description?' — '+esc(m.description):''}</small></div>`:''}
function applyModel(m,values={}){selectedModelId=m?.id||null;if(m){for(const [f,v] of [['name',m.name],['category',m.category],['manufacturer',m.manufacturer],['model',m.model_number]])if(form.elements[f]&&v)form.elements[f].value=v;showModel(m);renderDynamic(modelKeys(m),values)}else{showModel(null);renderDynamic(modelKeys(null),values)}}
function toggleLocationFields(){const meeting=document.querySelector('#locationType').value==='Meetingraum';['locationLabelField','locationSiteField','locationBuildingField','locationFloorField'].forEach(id=>{const e=document.querySelector('#'+id);if(e)e.hidden=meeting});const selector=document.querySelector('#roomSelectorWrap');if(selector)selector.hidden=!meeting||!!roomId}
async function chooseRoom(){if(roomId||entryId||document.querySelector('#roomSelectorWrap'))return;const rooms=await fetch('/api/rooms').then(r=>r.json());const realRooms=rooms.filter(r=>!String(r.category||'').startsWith('__ASSET_LOCATION__'));if(!realRooms.length){message('Bitte zuerst einen Meetingraum anlegen.');return}const wrap=document.createElement('div');wrap.id='roomSelectorWrap';wrap.className='field full';wrap.innerHTML='<label>Meetingraum *</label><select id="roomSelector" required><option value="">Bitte Raum auswählen</option></select>';form.querySelector('.formGrid')?.prepend(wrap);const s=wrap.querySelector('select');realRooms.forEach(r=>s.add(new Option(`${r.name} · ${[r.site,r.building,r.floor].filter(Boolean).join(' · ')}`,r.id)));s.onchange=()=>{roomId=Number(s.value)||0;toggleLocationFields()};}
async function setupModels(entry){const selector=document.querySelector('#modelSelector');models=await fetch('/api/equipment-models').then(r=>r.ok?r.json():[]);models.filter(m=>m.active!==false).forEach(m=>selector.add(new Option(`${m.manufacturer?m.manufacturer+' · ':''}${m.name}${m.model_number?' · '+m.model_number:''}`,m.id)));selector.onchange=()=>applyModel(models.find(m=>m.id===Number(selector.value)));form.elements.category.onchange=()=>{if(!selectedModelId)renderDynamic(modelKeys(null));};let metadata={};if(entry?.id){const [mr,dr]=await Promise.all([fetch(`/api/equipment/${entry.id}/model`),fetch(`/api/equipment/${entry.id}/metadata`)]);if(dr.ok)metadata=await dr.json();if(mr.ok){const a=await mr.json();if(a.model){selector.value=String(a.model.id);selectedModelId=a.model.id;showModel(a.model);renderDynamic(modelKeys(a.model),metadata.custom_fields||{})}}}const lt=document.querySelector('#locationType');lt.value=metadata.location_type||'Meetingraum';document.querySelector('#locationLabel').value=metadata.location_label||'';document.querySelector('#responsible').value=metadata.responsible||'';document.querySelector('#budgetCategory').value=metadata.custom_fields?.budget_category||'Meetingroom / Modernisierung';toggleLocationFields();lt.onchange=()=>{if(lt.value==='Meetingraum'&&!roomId)chooseRoom();toggleLocationFields()};if(!entry?.id)renderDynamic(modelKeys(null));}
async function resolveLocation(){const type=document.querySelector('#locationType').value;if(type==='Meetingraum'){if(!roomId){const selector=document.querySelector('#roomSelector');if(selector)roomId=Number(selector.value)||0}if(!roomId){message('Bitte einen Meetingraum auswählen.');return 0}return roomId}if(roomId&&String((await fetch(`/api/rooms/${roomId}`).then(r=>r.ok?r.json():null))?.category||'').startsWith('__ASSET_LOCATION__'))return roomId;const label=document.querySelector('#locationLabel').value.trim();if(!label){message('Bitte einen Standort / ein Büro angeben.');return 0}const r=await fetch('/api/asset-locations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:label,site:document.querySelector('#locationSite').value.trim(),building:document.querySelector('#locationBuilding').value.trim(),floor:document.querySelector('#locationFloor').value.trim(),location_type:type,owner:document.querySelector('#responsible').value.trim()})});if(!r.ok){message((await r.json().catch(()=>({}))).detail||'Standort konnte nicht angelegt werden.');return 0}roomId=Number((await r.json()).id);return roomId}
async function loadEntry(){if(!entryId){await setupModels(null);if(!roomId)await chooseRoom();return}if(!roomId){message('Für diesen Eintrag fehlt der Standort.');return}document.querySelector('#pageTitle').textContent=config.editTitle;const r=await fetch(`/api/rooms/${roomId}`);const room=await r.json(),entry=(room[config.collection]||[]).find(x=>x.id===entryId);if(!entry)throw Error('Der Eintrag wurde nicht gefunden.');config.fields.forEach(f=>{const i=form.elements[f.name];if(i)i.value=entry[f.name]??''});await setupModels(entry)}
form.addEventListener('submit',async e=>{e.preventDefault();if(!(await resolveLocation()))return;const data=Object.fromEntries(config.fields.map(f=>[f.name,f.type==='number'?(form.elements[f.name].value===''?null:Number(form.elements[f.name].value)):form.elements[f.name].value.trim()]));const url=entryId?`/api/${config.kind}/${entryId}`:`/api/rooms/${roomId}/${config.kind}`;const r=await fetch(url,{method:entryId?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok){message((await r.json().catch(()=>({}))).detail||'Speichern fehlgeschlagen.');return}const saved=await r.json();if(config.kind==='equipment'){if(selectedModelId)await fetch(`/api/equipment/${saved.id}/model?model_id=${selectedModelId}`,{method:'PUT'});const custom_fields=Object.fromEntries([...document.querySelectorAll('[data-dynamic]')].map(i=>[i.dataset.dynamic,i.value]));custom_fields.budget_category=document.querySelector('#budgetCategory').value;const md={location_type:document.querySelector('#locationType').value,location_label:document.querySelector('#locationLabel').value.trim(),responsible:document.querySelector('#responsible').value.trim(),custom_fields};await fetch(`/api/equipment/${saved.id}/metadata`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(md)});for(const [field,kind] of [['offerFile','offer'],['invoiceFile','invoice'],['imageFile','image']]){const file=form.elements[field]?.files?.[0];if(!file)continue;const fd=new FormData();fd.append('file',file);const u=await fetch(`/api/equipment/${saved.id}/documents?kind=${kind}`,{method:'POST',body:fd});if(!u.ok){message('Gerät gespeichert, aber eine Datei konnte nicht hochgeladen werden.');return}}}location.href=`/static/room-detail.html?id=${roomId}`});
document.querySelector('#cancel').onclick=()=>location.href=roomId?`/static/room-detail.html?id=${roomId}`:'/';loadEntry().catch(e=>message(e.message));