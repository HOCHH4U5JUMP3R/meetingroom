const searchInput=document.querySelector('#roomSearch'),filterContainer=document.querySelector('#siteFilters'),groupContainer=document.querySelector('#roomGroups'),emptyState=document.querySelector('#overviewEmpty'),errorState=document.querySelector('#overviewError');let allRooms=[],selectedSite='';const esc=v=>String(v==null||v===''?'–':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const siteFor=r=>r.site||'Ohne Standort',status=r=>String(r.status||'Aktiv').toLowerCase(),isBad=r=>status(r).includes('störung')||status(r).includes('außer')||Number(r.open_ticket_count||0)>=3,isWarn=r=>!isBad(r)&&(status(r).includes('modern')||Number(r.open_ticket_count||0)>0),roomLocation=r=>[r.building,r.floor&&`Etage ${r.floor}`,r.room_number&&`Raum ${r.room_number}`].filter(Boolean).join(' · ')||'Keine Standortdaten';function filteredRooms(){const q=searchInput.value.trim().toLowerCase();return allRooms.filter(r=>{if(selectedSite&&siteFor(r)!==selectedSite)return false;return[r.name,r.site,r.building,r.floor,r.room_number,r.category,r.connections,r.owner,r.host_name].join(' ').toLowerCase().includes(q)})}function renderDashboardMetrics(){const total=allRooms.length,open=allRooms.reduce((n,r)=>n+Number(r.open_ticket_count||0),0),bad=allRooms.filter(isBad).length,warn=allRooms.filter(isWarn).length,ok=Math.max(0,total-bad-warn);document.querySelector('#kpiRooms').textContent=total;document.querySelector('#kpiTickets').textContent=open;document.querySelector('#navTicketCount').textContent=open;document.querySelector('#kpiTicketHint').textContent=open?'Handlungsbedarf vorhanden':'alles ruhig';document.querySelector('#statusOk').textContent=ok;document.querySelector('#statusWarn').textContent=warn;document.querySelector('#statusBad').textContent=bad;document.querySelector('#statusOkBar').style.width=`${total?ok/total*100:0}%`;document.querySelector('#statusWarnBar').style.width=`${total?warn/total*100:0}%`;document.querySelector('#statusBadBar').style.width=`${total?bad/total*100:0}%`}
async function loadBudget(){try{const year=new Date().getFullYear();const res=await fetch(`/api/budget-overview?year=${year}`);if(!res.ok)return;const data=await res.json(),t=data.total||{},budget=Number(t.budget||0),spent=Number(t.spent||0),committed=Number(t.committed||0),available=Number(t.available??budget-committed),forecast=Number(t.forecast||0);document.querySelector('#kpiBudget').textContent=budget.toLocaleString('de-DE',{maximumFractionDigits:0})+' €';const used=budget?Math.min(100,committed/budget*100):0;document.querySelector('#kpiBudgetHint').textContent=`${used.toFixed(0)} % gebunden · ${year}`;document.querySelector('#budgetHero').textContent=available.toLocaleString('de-DE',{maximumFractionDigits:0})+' €';document.querySelector('#budgetSub').textContent=`${spent.toLocaleString('de-DE',{maximumFractionDigits:0})} € ausgegeben · ${forecast.toLocaleString('de-DE',{maximumFractionDigits:0})} € Forecast`;document.querySelector('#budgetProgress').style.width=`${used}%`}catch(_){}}
async function loadExtendedMetrics(){let projects=0;const attention=[];await Promise.all(allRooms.map(async r=>{try{const x=await fetch(`/api/rooms/${r.id}`).then(v=>v.json());projects+=(x.modernizations||[]).length;if((x.tickets||[]).some(t=>['Offen','In Bearbeitung'].includes(t.status))||isBad(x))attention.push(x)}catch(_){}}));document.querySelector('#kpiModern').textContent=projects;attention.sort((a,b)=>(b.open_ticket_count||0)-(a.open_ticket_count||0));const list=document.querySelector('#attentionList');if(!attention.length)list.innerHTML='<div class="attention-good"><strong>Alles im grünen Bereich</strong><span>Aktuell wurden keine kritischen Räume erkannt.</span></div>';else list.innerHTML=attention.slice(0,6).map(r=>`<a class="attention-row" href="/static/room-detail.html?id=${encodeURIComponent(r.id)}"><span class="attention-icon">!</span><div><strong>${esc(r.name)}</strong><small>${esc(roomLocation(r))}</small></div><span class="attention-reason">${Number(r.open_ticket_count||0)} offene Tickets</span><b>›</b></a>`).join('')}function renderFilters(){const sites=[...new Set(allRooms.map(siteFor))].sort();filterContainer.innerHTML='';['Alle',...sites].forEach(site=>{const b=document.createElement('button');b.type='button';b.className=`site-filter ${(!selectedSite&&site==='Alle')||site===selectedSite?'active':''}`;b.textContent=site;b.onclick=()=>{selectedSite=site==='Alle'?'':site;renderFilters();renderRooms()};filterContainer.appendChild(b)})}function renderRooms(){const rows=filteredRooms();document.querySelector('#roomResultText').textContent=`${rows.length} ${rows.length===1?'Raum':'Räume'}${selectedSite?` · ${selectedSite}`:' · alle Standorte'}`;emptyState.hidden=rows.length!==0;groupContainer.innerHTML='';const grouped={};rows.forEach(r=>(grouped[siteFor(r)]||=[]).push(r));Object.keys(grouped).sort().forEach(site=>{const section=document.createElement('div');section.className='v2-site-section';section.innerHTML=`<div class="v2-site-title"><span>${esc(site)}</span><small>${grouped[site].length} Räume</small></div><div class="v2-room-grid-inner"></div>`;const grid=section.querySelector('.v2-room-grid-inner');grouped[site].forEach(r=>{const state=isBad(r)?'danger':isWarn(r)?'warning':'success',image=r.image_url?`<img src="${esc(r.image_url)}" alt="Raumbild ${esc(r.name)}">`:'<div class="room-placeholder">⌂</div>';const card=document.createElement('a');card.className='v2-room-card';card.href=`/static/room-detail.html?id=${encodeURIComponent(r.id)}`;card.innerHTML=`<div class="v2-room-image">${image}<span class="room-health ${state}">${state==='success'?'OK':state==='warning'?'Hinweis':'Störung'}</span></div><div class="v2-room-body"><div class="v2-room-title"><strong>${esc(r.name)}</strong><span>›</span></div><p>${esc(roomLocation(r))}</p><div class="v2-room-meta"><span>◷ ${esc(r.seats??'–')} Plätze</span><span>⚑ ${Number(r.open_ticket_count||0)} offen</span></div><div class="v2-room-footer"><span>${esc(r.category||'Meetingraum')}</span><span>${esc(r.owner||'Kein Verantwortlicher')}</span></div></div>`;grid.appendChild(card)});groupContainer.appendChild(section)})}async function loadRooms(){try{const res=await fetch('/api/rooms',{headers:{Accept:'application/json'}});if(!res.ok)throw Error(`Räume konnten nicht geladen werden (HTTP ${res.status}).`);allRooms=await res.json();if(!Array.isArray(allRooms))throw Error('Ungültiges Raumdatenformat.');const initial=new URLSearchParams(location.search).get('q');if(initial){searchInput.value=initial}errorState.hidden=true;renderFilters();renderDashboardMetrics();renderRooms();loadBudget();loadExtendedMetrics()}catch(e){console.error(e);errorState.textContent=e.message;errorState.hidden=false;emptyState.hidden=true}}searchInput.addEventListener('input',renderRooms);loadRooms();