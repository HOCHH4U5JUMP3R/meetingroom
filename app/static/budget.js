const $=s=>document.querySelector(s);const globalForm=$('#globalBudgetForm'),globalInput=$('#globalBudget'),totals=$('#budgetTotals'),divisionTotals=$('#divisionTotals'),sites=$('#siteBudgets'),error=$('#budgetError'),yearSelect=$('#budgetYear'),quarterSelect=$('#budgetQuarter'),monthSelect=$('#budgetMonth');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const euro=v=>Number(v||0).toLocaleString('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0});const pct=v=>`${Number(v||0).toLocaleString('de-DE',{maximumFractionDigits:1})}%`;const showError=m=>{error.textContent=m;error.hidden=false};
function query(){const p=new URLSearchParams({year:yearSelect.value||new Date().getFullYear()});if(quarterSelect.value)p.set('quarter',quarterSelect.value);if(monthSelect.value)p.set('month',monthSelect.value);return p}
async function save(url,budget){const r=await fetch(url,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({budget:Number(budget)||0})});if(!r.ok)throw Error('Speichern fehlgeschlagen.');}
function moneyLine(label,value,cls=''){return `<div class="budget-metric ${cls}"><span>${esc(label)}</span><strong>${euro(value)}</strong></div>`}
function render(d){if(!yearSelect.options.length)(d.years||[]).forEach(y=>yearSelect.add(new Option(y,y)));yearSelect.value=String(d.year);globalInput.value=d.total.budget||'';$('#budgetKpi').textContent=euro(d.total.budget);$('#plannedKpi').textContent=euro(d.total.modernization_planned);$('#spentKpi').textContent=euro(d.total.spent);$('#availableKpi').textContent=euro(d.total.available);
totals.innerHTML=`<div class="budget-hero-grid">${moneyLine('Gesamtbudget',d.total.budget)}${moneyLine('Modernisierung geplant',d.total.modernization_planned)}${moneyLine('Modernisierung beauftragt',d.total.modernization_commissioned)}${moneyLine('Modernisierung Ist',d.total.modernization_spent)}${moneyLine('Equipment gekauft',d.total.equipment_spent)}${moneyLine('Gesamt ausgegeben',d.total.spent,'strong')}${moneyLine('Gesamt gebunden',d.total.committed)}${moneyLine('Forecast',d.total.forecast)}${moneyLine('Verfügbar',d.total.available,'strong')}</div><div class="budget-progress"><span style="width:${Math.min(100,d.total.utilization||0)}%"></span></div><small>${pct(d.total.utilization)} Budgetbindung · Equipment + Modernisierung automatisch aus den Stammdaten verrechnet</small>`;
divisionTotals.innerHTML=`<div class="division-grid">${(d.divisions||[]).map(g=>`<article class="division-card"><div class="division-head"><strong>${esc(g.division)}</strong><span>${g.sites} Standorte · ${g.rooms} Räume</span></div><div class="division-number">${euro(g.budget)}</div><div class="division-bar"><span style="width:${Math.min(100,g.utilization||0)}%"></span></div><div class="division-stats"><span>Gebunden <b>${euro(g.committed)}</b></span><span>Equipment <b>${euro(g.equipment_spent)}</b></span><span>Modernisierung <b>${euro(g.modernization_spent)}</b></span><span>Frei <b>${euro(g.available)}</b></span></div></article>`).join('')}</div>`;
sites.innerHTML=(d.sites||[]).map(s=>`<tr><td><div class="site-name"><span class="division-pill ${s.division==='DA'?'da':'dav'}">${esc(s.division)}</span><strong>${esc(s.site)}</strong></div><small>${s.rooms} Räume · ${pct(s.utilization)} gebunden</small></td><td>${s.rooms}</td><td><input class="site-budget-input" data-site="${encodeURIComponent(s.site)}" type="number" min="0" step="0.01" value="${s.budget||''}"></td><td>${euro(s.modernization_planned)}</td><td>${euro(s.committed)}</td><td>${euro(s.equipment_spent)}</td><td><strong>${euro(s.available)}</strong></td><td><button class="btn btn-small site-save" data-site="${encodeURIComponent(s.site)}">Speichern</button></td></tr>`).join('');
document.querySelectorAll('.site-save').forEach(b=>b.onclick=async()=>{try{const site=decodeURIComponent(b.dataset.site);const i=document.querySelector(`.site-budget-input[data-site="${b.dataset.site}"]`);await save(`/api/budget-overview/sites/${encodeURIComponent(site)}?year=${yearSelect.value}`,i.value);await load()}catch(e){showError(e.message)}})}
async function load(){error.hidden=true;const r=await fetch(`/api/budget-overview?${query()}`);if(!r.ok)throw Error('Budgetübersicht konnte nicht geladen werden.');render(await r.json())}
globalForm.onsubmit=async e=>{e.preventDefault();try{await save(`/api/budget-overview/global?year=${yearSelect.value}`,globalInput.value);await load()}catch(x){showError(x.message)}};[yearSelect,quarterSelect,monthSelect].forEach(x=>x.onchange=()=>load().catch(e=>showError(e.message)));load().catch(e=>showError(e.message));